import React from 'react';

export function BackgroundPattern({ opacity = 0.05, className = '' }: { opacity?: number; className?: string }) {
  // User requested to remove emoticons/icons and keep it simple.
  // Returning null effectively removes it.
  return null;
}
